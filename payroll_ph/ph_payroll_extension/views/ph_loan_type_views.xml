<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="view_ph_loan_type_list" model="ir.ui.view">
        <field name="name">ph.loan.type.list</field>
        <field name="model">ph.loan.type</field>
        <field name="arch" type="xml">
            <list string="Loan Types">
                <field name="sequence" widget="handle"/>
                <field name="name"/>
                <field name="code"/>
                <field name="is_government"/>
                <field name="input_type_id"/>
                <field name="active" column_invisible="1"/>
            </list>
        </field>
    </record>

    <record id="view_ph_loan_type_form" model="ir.ui.view">
        <field name="name">ph.loan.type.form</field>
        <field name="model">ph.loan.type</field>
        <field name="arch" type="xml">
            <form string="Loan Type">
                <sheet>
                    <group>
                        <field name="name"/>
                        <field name="code"/>
                        <field name="is_government"/>
                        <field name="input_type_id"/>
                        <field name="sequence"/>
                        <field name="active"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <record id="action_ph_loan_type" model="ir.actions.act_window">
        <field name="name">Loan Types</field>
        <field name="res_model">ph.loan.type</field>
        <field name="view_mode">list,form</field>
    </record>

</odoo>
